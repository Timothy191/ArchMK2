import { withPayload } from "@payloadcms/next/withPayload";

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any Next.js 15 specific config here if needed
};

export default withPayload(nextConfig);
