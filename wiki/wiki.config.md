title: "Arch-Mk2 Project Wiki"
flavor: research
scope: project
private: false

auto_research:
enabled: true
max_pages_per_run: 5
max_depth: 2
budget_usd: 0.50

# Notes

# This is a project-scoped wiki rooted at `wiki/` in the repository.

# Use `/wiki` commands (via the CLI skill) to seed, index, and run the research loop.
