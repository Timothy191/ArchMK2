version: '3.8'

services:
  cache-agent:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    command: ["pnpm", "start"]

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
